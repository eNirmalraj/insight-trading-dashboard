// @insight/computation — Expression Module Barrel Export
export {
    evaluateExpression,
    handleCrossover,
    evaluateComparison,
    getValue,
    getPreviousValue,
    validateExpression,
} from './evaluator';

export type { EvaluationContext } from './evaluator';
