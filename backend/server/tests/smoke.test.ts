// Smoke test to verify Jest + ts-jest is wired up correctly.
// Delete this file once the first real test lands.

describe('jest smoke', () => {
    it('runs a trivial assertion', () => {
        expect(1 + 1).toBe(2);
    });
});
